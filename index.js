const { 
   TruthTableGate,
   connect_gates } = require("./truth-table-gate.js");
const {
   NotGate,
   AndGate,
   OrGate,
   NandGate,
   NorGate
} = require("./basic-gates.js");

module.exports = {
   TruthTableGate,
   connect_gates,
   NotGate,
   AndGate,
   OrGate,
   NandGate,
   NorGate
};
